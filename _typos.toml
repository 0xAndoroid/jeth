# `typos` false positives: git SHA fragments such as `5abd70abf` in RESULTS.md.
[default]
extend-ignore-re = ["\\b[0-9a-f]{7,40}\\b"]

# Vendored upstream ark-ff (crates/vendor/ark-ff) keeps its own identifiers (`ba_add` etc.).
[files]
extend-exclude = ["crates/vendor/ark-ff/"]
