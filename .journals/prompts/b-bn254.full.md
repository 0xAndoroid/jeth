# Lane B-bn254: custom Jolt inlines for bn254 Fq multiplication (MULQ, SOPQ2, optionally FP2 mul) hooked into ark-ff for the ecmul/ecadd/pairing precompiles
LANE_WT=/Volumes/Dev/worktrees/jeth/inlines-b-bn254 · LANE_BRANCH=inlines-b-bn254 · LANE_TARGET=jeth-inlines-b-bn254 · FUNCT7=0x00 (opcode 0x2B)
You are a Jolt/jeth engineer. Start from the design that was written but never built: .journals/bn254-fq-inline-design.md (deterministic product-scanning Montgomery MULQ ≈ 285 rows vs ≈ 310 compiled, SOPQ2 ≈ 419 vs ≈ 520, FP2MULQ fused; register budget, column table, integration plan via a vendored ark-ff 0.5.0 hook on MontBackend::mul_assign / sum_of_products, jolt-core registration — replace that last part with this repo's crates/inlines/ convention + InlineExtension::External). Also compare against the advice-quotient style jolt-inlines/p256 uses (P256_MULQ 264 rows for a dense prime: a·b = w·q + c with c < q, w advised, checked with VirtualAssert rows) and pick the cheaper correct design per op; document the choice.
PRE-GATE FIRST (≤ 1 h): on block 25905781 (and 785, 789 which have 21 ecmul) measure dynamic rows/call for the ark Fq mul_assign, square_in_place and sum_of_products::<2> monomorphizations with jeth profile --rows --entries (the tool exists on this tree) and count calls; the design's GO rule: mul_assign ≥ 305 rows/call and sop2 ≥ 515 → build MULQ + SOPQ2; if lower, GO only as the package with the fused FP2 op; if the projected 781 saving is < 0.4% of rows AND the adversarial BN254 pairing c/g (150) / BN254MUL (129) would drop < 8%, NO-GO with numbers. Report the pre-gate numbers in your journal either way.
Integration: vendor registry ark-ff 0.5.0 into crates/vendor/ark-ff as ONE commit with the content UNCHANGED ("chore(vendor): ark-ff 0.5.0 for guest field hooks"), then a second commit with the hook: [patch.crates-io] ark-ff = { path = "../vendor/ark-ff" } in crates/guest/Cargo.toml AND the root workspace (native must be bit-identical to upstream: the hook is cfg(all(target_arch = "riscv64", feature = "jolt-bn254-inline"))); in montgomery_backend.rs's FpConfig impl for MontBackend<T, N>, dispatch by a const modulus check (N == 4 && T::MODULUS limbs == bn254 q) — one if-arm per field, so another lane can add its arm (BLS12-381 Fp, N = 6) with a trivial merge; do not restructure the file. jeth-core feature bn254-inline enables the crate + the ark-ff feature; guest enables it on the jeth-core dependency line. Verify the patch actually replaces ark-ff for revm-precompile AND jeth-core in the guest graph (cargo tree) and that the a16z arkworks fork used by jolt-sdk is untouched.
Gates: synth configs BN254ADD, BN254MUL/full-scalar, BN254MUL/scalar2, BN254PAIRING/k1, k2, k4, k8 (before/after rows, exact); ten blocks; 781/785/789 attribution (Fq mul rows before/after); forged blocks: ecadd/ecmul/pairing with points at infinity, invalid points, non-canonical coordinates ≥ q, scalar 0 / n / n+1 / 2^256−1, pairing with G1 infinity, invalid G2, empty input, the k=2 e(G1,G2)e(−G1,G2)=1 identity — guest hash == native hash on each (native = unhooked ark). InlineSpec tests: MULQ/SOPQ2 random 10k + edges {0, 1, R, R², q−1, (q−1)², u64::MAX limbs, cases forcing t ≥ q, a == b, rd aliasing rs1}. Golden row counts asserted.
## Repo, branches, paths (read before anything else)
- jeth repo: /Volumes/Dev/dev/jeth (~/dev is a symlink to it). NEVER touch that checkout (main), /Volumes/Dev/worktrees/jeth/amber-nolane, or /Volumes/Dev/worktrees/jeth/inlines-a / inlines-b (read their files only). Your worktree LANE_WT already exists on branch LANE_BRANCH (values given above); work only there. Base = branch inlines-b @ 6557a40 = amber-nolane + Phase A (PR #2: p256/sha2/blake2/bigint inlines wired) + jolt repin.
- Jolt pin (READ-ONLY worktree, never edit): /Volumes/Dev/worktrees/jolt/jolt-inlines-b, branch jolt-inlines-b on git@github.com:0xAndoroid/jolt-private.git = a16z/jolt main + amber cherry-picks + commit 3158917254 adding InlineExtension::External (the profile tag for inlines registered by downstream hosts; opcode 0x2B = custom-1 is the user-inline space). Reference inline crates: jolt-inlines/{secp256k1,p256,bigint,sha2,blake2,sdk,fixtures}; book chapter book/src/how/optimizations/inlines.md (read fully); expansion builder crates/jolt-program/src/expand/inline.rs + jolt-inlines/sdk/src/host.rs (InlineOp, InlineBuilderExt, MulAccExt, register_inlines!), jolt-inlines/sdk/src/spec.rs (InlineReference / InlineSpec test contract, feature test-utils). Never push to a16z/jolt. If the mechanism forces a jolt-side change, STOP and report it to the parent instead of editing.
- Per-lane build dirs (mandatory, export in every shell): CARGO_TARGET_DIR=/Volumes/Dev/cargo-target/LANE_TARGET  JETH_GUEST_TARGET_DIR=/Volumes/Dev/cargo-target/LANE_TARGET-guest  JOLT_PATH=/Volumes/Dev/cargo-target/jolt-cli-inlines-b/release/jolt (if that binary does not exist yet use /Volumes/Dev/cargo-target/jolt-cli-amber-nolane/release/jolt — identical guest build recipe).
- Build: cargo build -q --release -p jeth-host --features secp-inline (binaries $CARGO_TARGET_DIR/release/jeth and .../synth). First jeth trace builds the guest ELF pair (~10 min); --skip-build only when the guest did not change.
- Block inputs: COPY /Volumes/Dev/jeth-inputs/<block>/input.bin (25905781..25905790) into your own directory under /Volumes/Dev/cargo-target/LANE_TARGET-data/ before tracing — jeth trace writes trace-summary.json beside its input; never trace inside amber-nolane/data. Records (block_hash) = /Volumes/Dev/worktrees/jeth/amber-nolane/data/<block>/trace-summary.json. /tmp is wiped on daemon restarts: keep scratch under /Volumes/Dev/cargo-target/LANE_TARGET-scratch/.
- Baseline on inlines-b (exact, Phase A on): 781 601,992,101 rows (perms 115,373) · 782 715,141,963 · 783 333,107,908 · 784 250,985,973 · 785 628,588,538 · 786 291,810,444 · 787 395,621,866 · 788 92,464,391 · 789 676,244,012 · 790 432,649,242; gas-weighted 13.763942 c/g over 321,027,690 gas (gas per block: RESULTS.md last table). Keccak perms per block must stay equal.
- Commands: jeth run-native --input X (software reference) · jeth trace --input X [--skip-build] · jeth opcodes --input X --skip-build · jeth profile --rows [--entries SYMBOL ...] [--pcs-of SYMBOL] --input X (rows and entry counts per symbol → rows/call) · RESULTS.md 'Reproduce'.
- Tests: cargo nextest run --cargo-quiet --release --workspace --features jeth-host/secp-inline. Pre-commit runs fmt + clippy -D warnings; the typos hook has pre-existing failures in untouched files — DISABLE_TYPOS=1 git commit only after fixing typos in files YOU changed.

## Custom inline crate conventions (this repo)
- Crate at crates/inlines/<name>/ (workspace member), package name jeth-inlines-<name>. Mirror jolt-inlines/secp256k1 (or p256) file shape: lib.rs (consts: INLINE_OPCODE = 0x2B, FUNCT7 = the value assigned to your lane below, one FUNCT3 per op + NAME consts), sdk.rs (guest .insn wrappers behind cfg(target_arch = "riscv64"), host/native software fallbacks that ARE the reference semantics), host.rs (feature host: jolt_inlines_sdk::register_inlines! { trace_file: "<name>_trace.joltinline", extension: jolt_inlines_sdk::host::InlineExtension::External, ops: [...] }), sequence_builder.rs (InlineOp impls using InlineExpansionBuilder + InlineBuilderExt; all inline registers released before finalize; deterministic unless you document advice + VirtualAssert rows), spec.rs (InlineReference + InlineSpec with edge_cases() and random(); dev-dep jolt-inlines-sdk features test-utils; assert_random_cases_match_reference::<Op>(seed, 10_000) + assert_edge_cases_match_reference), a golden row_count test asserting the exact rows of every op's build_sequence (like jolt-inlines/fixtures), and a structural test of constants (modulus limbs, INV, bounds).
- Deps: jolt-inlines-sdk = { path = "/Volumes/Dev/worktrees/jolt/jolt-inlines-b/jolt-inlines/sdk", default-features = false } (+ features = ["host"] under the host feature; test-utils in dev-deps); tracer path dep for tests only. Guest: default-features = false.
- Wiring: crates/host/Cargo.toml dep with features = ["host"] + extern crate jeth_inlines_<name> as _; in crates/host/src/trace.rs (inventory registration; without it the emulator panics on the unknown insn). Guest-only routing behind a jeth-core feature <name>-inline enabled on the crates/guest/Cargo.toml jeth-core dependency line (same pattern as sha2-inline / p256-inline). Native (run-native) MUST keep the software path.
- FUNCT7 assignment (opcode 0x2B): bn254 = 0x00, bls12-381 = 0x01, blake2f rounds = 0x02 (0x03 if you need a second funct7 block).
- Memory contract discipline: 8-byte-aligned u64 limb arrays, all loads before stores when rd may alias rs1/rs2, document which words are read/written.

## Deliverables of every Phase B lane (all mandatory before you report)
1. Spec in LANE_WT/.journals/lanes/LANE_BRANCH.md: op semantics, register/memory contract, cycle budget (rows per op; golden numbers), and a soundness argument: the inline's row sequence computes exactly the op's semantics on ALL inputs it can receive (domain stated; edge cases: 0, 1, p−1, values ≥ p if reachable, aliasing); if advice is used, list every assert row and what a dishonest prover can and cannot do.
2. Tests: InlineSpec random (≥ 10k) + edge vs the software reference through the tracer harness; golden row counts; jeth-level differential tests vs the unhooked software path on random + adversarial inputs.
3. Synth harness configs for the affected precompiles/ops (rows before/after, exact; before = same tree feature off) — the harness lives in .journals/opcode-max-cg/ (copy the scripts to your scratch dir, point them at your binaries; run with uv run python; read .journals/opcode-max-cg-2026-09.md and the lane copies in .journals/lanes/inlines-a-*.md for the per-tx method).
4. Ten-block set: rows per block before/after, block_hash == record on all ten, keccak perms equal to baseline on all ten.
5. Block 781 attribution (jeth profile --rows, feature off vs on).
6. Forged/edge synth blocks with the precompile output SSTOREd: guest hash == native hash.
7. Workspace nextest green; pre-commit green on every commit.

## Rules
- Conventional Commits, small commits on LANE_BRANCH only; never commit on inlines-a/inlines-b/amber-nolane/main; do not push.
- Complexity budget: smallest diff; no speculative abstractions; no narration comments; no backwards-compat shims. Generic wording in code/comments (no campaign or lane names).
- Vault ~/.pika/vault read-only. Never edit anything under /Volumes/Dev/worktrees/jolt. Never raise MAX_SUFFIXES; no new lookup tables / instruction kinds (the inline must be built from existing RV64 + virtual instructions).
- Do not run 'git merge ... | tail'. uv for python (never bare python3). Use rip instead of rm.
- Kill rule: if your pre-gate or first measurement shows the inline cannot beat the compiled code by a meaningful margin (state your bar up front, e.g. ≥ 10% of the op's rows or ≥ 0.3% of block 781), STOP building, write the negative result with exact numbers to the lane journal, and report — a clean measured negative result is a valid outcome.
- Finish with ONE pika-cli reply parent --text: branch + head sha, verdict (built / killed), golden rows per op, gate tables (synth before/after, 10-block rows + hash-ok + perms-ok, 781 attribution), forged-input results, files changed, unsafe added, soundness summary, open questions. No buttons, terse.
