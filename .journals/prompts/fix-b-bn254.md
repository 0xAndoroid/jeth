Review of inlines-b-bn254 @ 419da11 came back APPROVE with should-fixes. In /Volumes/Dev/worktrees/jeth/inlines-b-bn254 (no CARGO_TARGET_DIR; JETH_GUEST_TARGET_DIR=/Volumes/Dev/worktrees/jeth/inlines-b-bn254/target/guest; JOLT_PATH=/Volumes/Dev/worktrees/jolt/jolt-inlines-b/target/release/jolt):
STEP 1 — one fix commit:
F1 crates/inlines/bn254/src/tests.rs: add direct sequence-vs-upstream-ark tests (random 20k quadruples + the adversarial triple set {0, 1, R, R², q−1, q−2, (q−1)³, −1, 2, 1⁻¹, 2^k, saturated limbs}) for all three ops, plus a structural test over the emitted rows (every rd is a virtual register, LD bases ∈ {rs1, rs2}, SD base = rd operand, last LD before first SD, zero advice/assert rows, reset set == written set). The reviewer's ready-made version is at /tmp/bn254-scratch/src/review_tests.rs if it still exists (/tmp may have been wiped — if so, write them; keep them compact).
F2 crates/vendor/ark-ff/src/jolt_bn254.rs:41,53: debug_assert on size_of::<F>() == 32 / M == 2 → assert! (const-folds when true, real guard otherwise).
F3 crates/inlines/bn254/src/spec.rs:86: the edge element [q0−1, MAX, MAX, q3] is ≥ q; replace with an in-domain corner [q0−1, q1, q2−1, q3] and move the ≥ q value to the out-of-domain test.
N7: pin the Fq2-mul row economics with a golden count or a one-line comment where is_fq2 relies on const folding.
Journal nit N6: the tight bound is t < 1.378q (2q is what the caller needs) — fix the wording.
Run the workspace nextest; pre-commit green.
STEP 2 — rebase inlines-b-bn254 onto branch inlines-b (now @ a7532a9 + docs commits: it contains the merged blake2f lane, the .cargo/config.toml removal 3c8658c — your 92018e0 duplicate should drop out or resolve trivially — and per-worktree target defaults in trace.rs). Resolve conflicts as unions (workspace members + deps, host Cargo + trace.rs extern crates, guest Cargo features, crypto.rs). Then: workspace nextest; rebuild the guest and trace /Volumes/Dev/jeth-scratch/inlines-b-bn254/data copies of 25905781 (expected 596,202,738 rows if additive with blake2f's Δ0, hash 0xf691da3f…, perms 115,373) and all ten blocks; report the rows.
Do not push. Finish with ONE pika-cli reply parent --text: rebased head sha, test count, ten-block rows + hash-ok + perms-ok, what changed.
