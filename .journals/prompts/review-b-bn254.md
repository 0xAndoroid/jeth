# Fresh adversarial review: lane inlines-b-bn254 (custom Jolt Montgomery inlines for bn254 Fq hooked into a vendored ark-ff)
Independent reviewer (you did not write this). Consensus-critical: every bn254 precompile (ecadd 0x06, ecmul 0x07, pairing 0x08) on the guest now runs its Fq arithmetic through hand-written Jolt inline sequences (MULQ 273 rows, SOPQ2 415, FP2MULQ 797; deterministic product-scanning Montgomery, output in [0, 2q) canonicalized by ark's subtract_modulus) via a vendored ark-ff 0.5.0 with cfg'd hook arms. Any input where the guest field result differs from upstream ark-ff is a chain split. Posture: presumptive blockers PLUS soundness — the sequence itself, the Montgomery identity, the domain assumptions, the unsafe glue.
Where: worktree /Volumes/Dev/worktrees/jeth/inlines-b-bn254, branch inlines-b-bn254 @ 419da11; base = 6557a40 (inlines-b). Review 'git diff 6557a40..419da11' — note e11b444 vendors registry ark-ff 0.5.0 byte-identical (verify: diff -r against ~/.cargo/registry/src/*/ark-ff-0.5.0/src, ignoring Cargo.toml.orig/.cargo_vcs_info) so you can focus on the hook delta; read .journals/lanes/inlines-b-bn254.md and .journals/bn254-fq-inline-design.md. Do NOT modify or commit there. Build/test in that worktree without CARGO_TARGET_DIR (it builds into its own target/): cargo nextest run --cargo-quiet --release --workspace --features jeth-host/secp-inline. Read-only references: /Volumes/Dev/worktrees/jolt/jolt-inlines-b/{jolt-inlines/secp256k1/src/sequence_builder.rs, jolt-inlines/sdk/src/host.rs (MulAccExt row rules), crates/jolt-program/src/expand/inline.rs, book/src/how/optimizations/inlines.md}; ark-ff-macros 0.5.0 montgomery/mul.rs (the CIOS the hook replaces) and ark-ff montgomery_backend.rs / sum_of_products.rs / quadratic_extension.rs upstream.
Verify by reading code and by running your own differential checks (a /tmp or scratch copy; do not commit):
1. Montgomery correctness: for a, b < q, the sequence computes t = (a·b + m·q) / 2^256 with m_k = r_k · INV mod 2^64 per column, INV = −q^−1 mod 2^64; the trick 'lo(m_k·q_0) replaced by the exact carry [r_k ≠ 0]' — prove r_k + lo(m_k·q_0) ≡ 0 mod 2^64 and that the carry-out is exactly [r_k ≠ 0]; column carry bounds (max terms per column × 2^64 fits the 2-register accumulator, no overflow of the carry counter); t < 2q < 2^255 so no column-7 carry (state the bound: (q² + (2^256 − 1)·q)/2^256 < 2q). Output in [0, 2q): does EVERY caller canonicalize? Check each hooked path: mul_assign, sum_of_products (the chunking bound: ark fuses M ≤ 3 terms for 254-bit q — the inline's SOPQ2 handles exactly M == 2; what happens for M == 1 or 3, which path?), Fp2 mul (quadratic_extension.rs arm: Karatsuba vs schoolbook, the NONRESIDUE = −1 handling, the sub/negation of the c1 · d1 term — all in [0, q) before and after?), square_in_place (left compiled — confirm no hook), from_bigint/into_bigint, and any call site that reads the Fp representation before subtract_modulus runs (PartialEq, is_zero, serialization, hashing) — a non-canonical intermediate escaping would be a consensus bug.
2. SOPQ2 bound: t < (a0·b0 + a1·b1 + m·q)/2^256 — with two products the numerator can reach 2q² + 2^256·q; show t < 2q still holds (or the output range the caller's single subtract_modulus assumes), for all a_i, b_i < q. Same for FP2MULQ's internal accumulations (the c0 = a0b0 − a1b1 term: how is the subtraction done modularly inside the inline — add q·k before subtracting? range of the result?).
3. The sequence: register allocation (all inline regs released, resets emitted, no real register written, rd usage), all loads before stores (rd aliasing rs1/rs2 claimed OK), memory contract (32-byte a, b; 64-byte for SOPQ2/FP2 inputs; alignment), no advice/assert rows (deterministic). Golden row counts pinned. Check the exec/model used as the InlineSpec reference is independent of the sequence (not the same code).
4. ark-ff hook delta (montgomery_backend.rs, quadratic_extension.rs, jolt_bn254.rs): cfg(all(target_arch = "riscv64", feature = "jolt-bn254-inline")) so native is bit-identical to upstream; the const modulus check picks exactly bn254 Fq (N == 4 and all four limbs) — does it accidentally match bn254 Fr or any other 4-limb field? (Fr shares two limbs with Fq!) The unsafe casts: Fp<P, N> and BigInt<N> layout (repr?), QuadExtField layout (repr(C)? field order c0, c1?), size_of/offset_of asserts, MaybeUninit fully written before assume_init, the *mut u64 → &mut BigInt<4> for subtract_modulus (aliasing/provenance). Is the [patch.crates-io] applied in BOTH the guest package and the root workspace, and does cargo tree show a single ark-ff 0.5.0 for revm-precompile + jeth-core + ark-bn254/ec/poly in the guest graph? The a16z arkworks fork used by jolt-sdk must be untouched.
5. Prover-side failure modes: none should exist (deterministic); confirm no panics reachable on valid input (asserts in the hook, debug_asserts that become release asserts).
6. Tests: 10k random × 3 ops + edge cartesian via the tracer harness against a model, 20k model-vs-ark, forged block 62 txs — are the references independent (ark upstream, not the model)? Anything committed that should not be; complexity budget (jolt_bn254.rs size, wrappers, narration comments).
Output: verdict (APPROVE / FIX-THEN-RE-REVIEW / BLOCK) + numbered findings with file:line, severity (blocker / should-fix / nit), evidence, concrete fix. Terse. Finish with ONE pika-cli reply parent --text. Vault read-only. Never edit under /Volumes/Dev/worktrees/jolt. Never set CARGO_TARGET_DIR.
