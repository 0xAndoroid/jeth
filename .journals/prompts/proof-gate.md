# Lane PROOF-GATE: generate and verify a real Jolt proof of a guest that runs the three custom jeth inlines (bn254 pairing, a BLS12-381 op, BLAKE2F)
You are a Jolt/jeth engineer. Everything measured so far in the inlines campaign is execute-only (tracer rows + block hashes). The operator requires, before anything merges: a Jolt example guest exercising ONE bn254 pairing + ONE BLS12-381 op (G2 add or a small G2 MSM — must hit Fp2 mul, i.e. the fused FP2MUL/SOPP2 ops) + ONE BLAKE2F compression with rounds not equal to 12 (e.g. 20 → FULL10 ×2, and one with rounds = 13 → FULL10 + PREFIX_3) through the custom inlines, and a REAL proof generated AND verified (jolt prove + verify, not just tracing). If proving or verifying fails on any inline sequence, that op's lane is blocked: isolate which op (build one guest function per op if needed) and report the failing op with the error.
Where the inlines live (READ-ONLY, do not edit): jeth branch inlines-b, worktree /Volumes/Dev/worktrees/jeth/inlines-b — crates/inlines/bn254 (jeth-inlines-bn254: MULQ/SOPQ2/FP2MULQ, opcode 0x2B funct7 0x00), crates/inlines/bls12-381 (jeth-inlines-bls12-381: MULP/SOPP2/FP2MUL, funct7 0x01), crates/inlines/blake2f (jeth-inlines-blake2f: FULL10/PREFIX_r, funct7 0x02/0x03; jeth_inlines_blake2f::compress(rounds, h, m, t, f) is the full BLAKE2F), crates/vendor/ark-ff (ark-ff 0.5.0 + cfg'd hook arms behind features jolt-bn254-inline / jolt-bls12-381-inline, target riscv64 only) — read their Cargo.toml features, lib.rs/sdk.rs/host.rs and the lane journals .journals/lanes/inlines-b-{bn254,bls,blake2f}.md first; see how jeth wires them (crates/host/src/trace.rs extern crate lines with the host feature for inventory registration; crates/guest/Cargo.toml [patch.crates-io] ark-ff = path and the jeth-core feature flags).
Where you work: the jolt-private worktree /Volumes/Dev/worktrees/jolt/jolt-inlines-b (branch jolt-inlines-b, remote 'private' = git@github.com:0xAndoroid/jolt-private.git; NEVER push to origin = a16z/jolt). Add examples/jeth-inlines-proof-gate/ modelled on examples/p256-ecdsa-verify (host crate + guest crate with #[jolt::provable] functions; the host compiles the guest, preprocesses, proves, verifies). The examples are members of the jolt workspace (root Cargo.toml [workspace] members list) — decide whether to add yours as members or make it a standalone workspace (own [workspace] table with path deps on ../../jolt-sdk etc.), whichever lets you (a) depend on the three jeth crates by ABSOLUTE path, (b) apply [patch.crates-io] ark-ff = { path = "/Volumes/Dev/worktrees/jeth/inlines-b/crates/vendor/ark-ff" } with the two hook features so ark-bn254 0.5 / ark-bls12-381 0.5 pairing/G2 code in the guest goes through the inlines, (c) not disturb the rest of the jolt workspace (jolt-sdk uses the a16z arkworks fork from git — that source must stay untouched; check cargo tree). If the workspace-member route forces a patch on the whole jolt workspace, use the standalone-workspace route. Build dirs: NEVER set CARGO_TARGET_DIR; the example builds into its own target/ (the p256 example writes guest targets to /tmp/jolt-guest-targets — use a path under the worktree instead, e.g. target/guest, since /tmp is wiped on restarts).
Guest content: fn proof_gate(inputs) -> outputs computing (1) one bn254 pairing check e(P, Q) · e(−P, Q) == 1 using ark-bn254 (Bn254::multi_pairing over two pairs → result is one; also output the raw Fq12 or a hash of it), (2) one BLS12-381 G2 point addition (and, if cheap, a 2-point G2 MSM) using ark-bls12-381, output the affine coordinates, (3) BLAKE2F with rounds = 20 and rounds = 13 through jeth_inlines_blake2f::compress (EIP-152-style h/m/t/f), output h. Keep the guest small enough to prove on this Mac (bn254 pairing k=2 ≈ 13M cycles after the inlines; check the tracer cycle count first; if memory is a problem prove the ops as separate #[jolt::provable] functions — a bn254 pairing alone, a BLS G2 add alone, BLAKE2F alone — that is acceptable and actually more informative). The HOST must (a) link the three jeth crates with their host feature (extern crate … as _;) so the tracer/prover know the 0x2B opcodes, (b) check outputs against native arkworks / revm-style software results, (c) run prove then verify and assert verification succeeds, (d) print cycle counts and prove/verify wall time. Also confirm which instruction profile the prover uses and that InlineExtension::External is in it (crates/jolt-riscv/src/profile.rs); if the prover path validates the profile fingerprint / registered inlines anywhere else (grep for supports_inline, inline_extensions, fingerprint in jolt-core and jolt-sdk), make sure the External extension is accepted end-to-end. Also run the existing jolt inline fixture/harness tests if they enumerate registered inlines (cargo nextest -p jolt-inlines-fixtures --features host) to see whether the new opcode space upsets any golden file — do NOT change golden files without reporting.
Deliverables: (1) the example committed on jolt-inlines-b (Conventional Commits; small; generic wording) and pushed to the private remote; (2) a lane journal at /Volumes/Dev/worktrees/jeth/inlines-b/.journals/lanes/proof-gate.md — NO: you must not commit in the jeth worktree; write the journal to /Volumes/Dev/jeth-scratch/proof-gate/proof-gate.md instead and I will bank it; (3) ONE pika-cli reply parent --text with: PASS/FAIL per op (bn254 pairing, BLS G2 op, BLAKE2F r20 + r13), cycle counts, prove and verify wall times and peak memory if measurable, the exact commands to reproduce, the commit sha on jolt-inlines-b, and any golden-file or profile issue found. Vault read-only. Never set CARGO_TARGET_DIR. Never edit files under /Volumes/Dev/worktrees/jeth (read only).
