# Fresh adversarial review: lane inlines-b-bls (custom Jolt Montgomery inlines for BLS12-381 Fp, 6 limbs, hooked into a vendored ark-ff)
Independent reviewer (you did not write this). Consensus-critical: every BLS12-381 precompile (EIP-2537 G1/G2 add, MSM, pairing, map-to-curve) and the KZG point-evaluation precompile on the guest now run their Fp arithmetic through hand-written Jolt inline sequences (MULP 647 rows, SOPP2 957, FP2MUL 1,875; deterministic product-scanning Montgomery over 6 limbs with an in-inline branch-free conditional subtraction, output claimed canonical < p) via a vendored ark-ff 0.5.0 with cfg'd hook arms. Any input where the guest field result differs from upstream ark-ff is a chain split. Posture: presumptive blockers PLUS soundness — the sequence itself, the Montgomery identity and bounds, the domain assumptions, the unsafe glue.
Where: worktree /Volumes/Dev/worktrees/jeth/inlines-b-bls, branch inlines-b-bls @ 00fe5f6; base = 6557a40 (inlines-b). Review 'git diff 6557a40..00fe5f6' — 3bf6e2c vendors registry ark-ff 0.5.0 (verify byte-identical: diff -r against ~/.cargo/registry/src/*/ark-ff-0.5.0, ignoring .cargo-ok / Cargo.toml.orig / .cargo_vcs_info) so you can focus on the hook delta; read .journals/lanes/inlines-b-bls.md. Do NOT modify or commit there. Build/test without CARGO_TARGET_DIR (the worktree builds into its own target/): cargo nextest run --cargo-quiet --release --workspace --features jeth-host/secp-inline. Read-only references: /Volumes/Dev/worktrees/jolt/jolt-inlines-b/{jolt-inlines/secp256k1/src/sequence_builder.rs, jolt-inlines/sdk/src/host.rs (MulAccExt row rules), crates/jolt-program/src/expand/inline.rs, book/src/how/optimizations/inlines.md}; ark-ff-macros 0.5.0 montgomery/mul.rs (the CIOS being replaced; note for 381-bit p the no-carry optimization condition and what ark does), ark-ff montgomery_backend.rs / sum_of_products.rs / quadratic_extension.rs upstream, ark-bls12-381 0.5 fq.rs / fq2.rs (NONRESIDUE, Fp2 layout).
Verify by reading code and by running your own differential checks (scratch copy; do not commit):
1. Montgomery correctness for 6 limbs: t = (a·b + m·p) / 2^384 with per-column m_k = r_k · INV mod 2^64, INV = −p^−1 mod 2^64; the 'lo(m_k·p_0) replaced by the carry [r_k ≠ 0]' trick; column carry bounds (≤ 23 terms claimed — check the actual max column population for MULP, SOPP2 and FP2MUL and that the 2-register accumulator cannot overflow); the top column: with p ≈ 2^381 (p < 2^381) show t < 2p and that no carry escapes column 11 (bound for SOPP2: (2p² + 2^384·p)/2^384 < 2p requires 2p < 2^384 — fine — but check the claimed tighter 1.125p / 1.25p bounds are not load-bearing beyond a single conditional subtraction). The branch-free conditional subtraction: mask = sign of (t − p) over 384 bits — verify the borrow chain and the select produce t − p when t ≥ p and t otherwise, including t == p exactly.
2. The claim 'also correct with one operand ≥ p (a·b < pR)': check the arithmetic; identify exactly which callers can present a non-canonical operand (the FP2MUL internal p − b1 = p case when b1 = 0 — trace it) and that the result is still canonical there. Fp2 mul arm: which formula (Karatsuba: (a0+a1)(b0+b1) − a0b0 − a1b1 with NONRESIDUE −1 → c0 = a0b0 − a1b1, c1 = a0b1 + a1b0)? How is c0's subtraction done modularly inside the inline (p − b1 then sum-of-products?) and is every intermediate < p where the sequence assumes it? Does FP2MUL fall back correctly when the QuadExtConfig is not the expected one (size 48/96, offsets 0/48, NONRESIDUE −1 checks are compile-time consts — verify they cannot be satisfied by a different tower)?
3. sum_of_products: ark's fused chunk size for 381-bit p is 2·(384−381)−1 = 5; which M values does ark actually call in the pairing/G2 code, and what does the hook do for M ≠ 2 (fall through to ark? correct chunking?). Every hooked path (mul_assign, sum_of_products, Fp2 mul) must leave canonical values; square_in_place left compiled — confirm no hook. Any path that reads the representation before canonicalization (PartialEq, is_zero, serialize, hash)?
4. The sequence: register allocation (27/39/46 regs, all released, resets emitted), no real register written, all loads before stores (rd aliasing), memory contract (48-byte operands, 96-byte for SOPP2/FP2 pairs; alignment), no advice/assert rows. Golden row counts pinned; the InlineSpec reference is an independent big-int model (not the sequence) and the 1k-vs-arkworks test uses upstream ark.
5. ark-ff hook delta (montgomery_backend.rs +13, quadratic_extension.rs +4, jolt_bls12_381.rs): cfg(all(target_arch = "riscv64", feature = ...)) so native is bit-identical to upstream; const check N == 6 ∧ all six MODULUS limbs == p (no other 6-limb field can match); the bn254 lane adds an N == 4 arm to the same if-chain — is the structure a clean chain of independent arms? Unsafe: Fp/BigInt/QuadExtField layout assumptions (repr, field order c0/c1), size/offset asserts, MaybeUninit fully written, pointer casts and aliasing. [patch.crates-io] in both guest and root; cargo tree shows one ark-ff 0.5.0 for revm-precompile bls12_381 + kzg + ark-bls12-381 in the guest graph; a16z arkworks fork untouched. Confirm revm-precompile 34 really uses ark (not blst / c-kzg) for BLS and KZG in the guest build.
6. Prover-side failure modes: none (deterministic); no panics reachable on valid input.
7. Tests/complexity: forged block (199 EIP-2537 + KZG cases) references independent; nothing committed that should not be; jolt_bls12_381.rs size, wrappers, narration comments.
Output: verdict (APPROVE / FIX-THEN-RE-REVIEW / BLOCK) + numbered findings with file:line, severity (blocker / should-fix / nit), evidence, concrete fix. Terse. Finish with ONE pika-cli reply parent --text. Vault read-only. Never edit under /Volumes/Dev/worktrees/jolt. Never set CARGO_TARGET_DIR.
