# Write the final HTML report of the jeth inlines campaign
Use the html-report-design skill at ~/.dotfiles/claude/skills/html-report-design/ (read SKILL.md and DESIGN.md fully; copy skeleton-dashboard.html — this is data-forward; fill TITLE / TAGS / CONTENT; run lint.py until 0 errors). Output: ~/.pika/web/reports/jeth-inlines-campaign-2026-09.html with <meta name="tags" content="jolt, jeth, benchmark">. Lean: verdict + tables/numbers first, no narrative history, no re-derivable context, scannable cards/tables. No images. Also use the dataviz skill rules for any chart (JSON-driven charts per the skeleton; at most 2 charts: the per-op c/g before/after bars and the 10-block c/g ladder).
Sources (read-only): /Volumes/Dev/worktrees/jeth/inlines-b/RESULTS.md — the last two sections 'Campaign inlines-a' and 'Campaign inlines-b' (they contain every number: per-op rows/c/g before→after, ten-block tables, Aztec block, gates, proof gate, review status); the lane journals /Volumes/Dev/worktrees/jeth/inlines-b/.journals/lanes/{inlines-a-p256,inlines-a-hash,inlines-a-bigint,inlines-b-bn254,inlines-b-bls,inlines-b-blake2f,proof-gate}.md (rows/call, unsafe inventory, soundness arguments, review findings); the campaign journal .journals/jeth-inlines-campaign.md (review task ids/verdicts); the max-c/g study CSV ~/.pika/web/reports/jeth-opcode-max-cycles-per-gas-2026-09.csv ('before' adversarial bounds: unit_cg column; adversarial 60M-gas block rows = unit_rows × 60e6 / unit_gas). PRs: https://github.com/0xAndoroid/jeth/pull/2 (Phase A, inlines-a → amber-nolane) and https://github.com/0xAndoroid/jeth/pull/3 (Phase B, inlines-b → inlines-a); jolt-private branch jolt-inlines-b (one commit InlineExtension::External + the proof-gate example).
Required content, in this order:
1. Verdict strip: ten-block c/g 13.814672 (amber-nolane) → 13.763942 (inlines-a) → 13.653997 (inlines-b); worst adversarial op 539 c/g (P256VERIFY) → the new worst (compute from the after numbers: BLS_G2MSM k1 358.8; list top 5 after); proof gate PASS (bn254 pairing / BLS G2 / BLAKE2F proven+verified); PR links.
2. Per-op table: op, c/g before → after, rows/unit before → after, adversarial 60M-gas block rows before → after, mechanism (stock jolt inline wired / custom inline), phase. Ops: P256VERIFY, SHA256 (8 KiB, 1 KiB, 32 B), BLAKE2F (r12, r13, r20, r100, r1000), MULMOD, MODEXP (32-32-32, 32-e1-32, 16-32-16), BN254PAIRING k1/k2/k4/k8, BN254MUL, BN254ADD, BLS_G2MSM k1/k8, BLS_PAIRING k1/k2, POINTEVAL, BLS_MAP_FP2_TO_G2, BLS_G2ADD, BLS_G1MSM, BLS_G1ADD. Mark unchanged ops honestly (MUL, EXP, ADDMOD measured and not routed; MODEXP ≥ 64 B / even moduli; BLAKE2F r0).
3. Ten-block ladder table (per block: gas, rows amber-nolane / inlines-a / inlines-b, c/g each, perms) + Aztec block 25694235 row (1,426,158,765 → 1,375,135,731, −3.58%) — with the honest note that the set has no P256VERIFY/BLAKE2F/BLS calls, so the set-wide win is small (−1.16%) and the campaign's value is the adversarial bound.
4. 'What each inline cost' table: inline op(s), rows/call (golden), replaces (compiled rows/call), unsafe added (from the lane journals/reports: p256 none; hash one asm! block; bigint extern "C" hook + raw-pointer inline calls; blake2f one asm!; bn254 asm blocks + jolt_bn254.rs casts; bls 3 unsafe fns + 4 blocks), soundness review status (reviewer verdict, rounds, key finding fixed: p256 z=0 rewrite pinned; hash nits; bigint S1–S3; blake2f const-assert R; bn254 direct seq-vs-ark tests + assert!; bls const guard after the proof-gate finding), tests (counts).
5. Proof gate card: what was proven, cycles, prove/verify times, RSS, the finding it surfaced (BLS guard not folding) and the fix.
6. Not done / follow-ups: BLS fused Fp2 squaring; BLAKE2b FULL20; jolt-inlines-p256 limb compares (8% of P256VERIFY); MODEXP big limbs killed (inline = compiled rows/partial product); Fr/squaring inlines never pay.
Keep it under ~250 lines of content; no personal data; no campaign-internal ids beyond PR numbers and shas. Finish with ONE pika-cli reply parent --text giving the report path, lint result (0 errors), and the top-5 adversarial ops after. Vault read-only. Do not modify any repo.
