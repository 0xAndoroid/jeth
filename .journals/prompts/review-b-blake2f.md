# Fresh adversarial review: lane inlines-b-blake2f (custom Jolt inline family for BLAKE2F rounds, opcode 0x2B)
Independent reviewer (you did not write this). Consensus-critical: BLAKE2F (0x09) output on the guest must equal revm-precompile 34's software compress for every (rounds, h, m, t, f). This is the first CUSTOM inline in jeth (a hand-written Jolt inline sequence, not a stock crate), so the review must cover the sequence itself, not just the glue. Posture: presumptive blockers (incidental complexity, single-call-site abstractions, just-in-case paths) PLUS soundness.
Where: worktree /Volumes/Dev/worktrees/jeth/inlines-b-blake2f, branch inlines-b-blake2f @ 3fb9b6a; base = 6557a40 (inlines-b). Review 'git diff 6557a40..3fb9b6a' and read .journals/lanes/inlines-b-blake2f.md. Do NOT modify or commit there. Build/test in that worktree WITHOUT CARGO_TARGET_DIR (it builds into its own target/; if .cargo/config.toml still points at /Volumes/Dev/cargo-target, pass --target-dir target to cargo): cargo nextest run --cargo-quiet --release --workspace --features jeth-host/secp-inline. Read-only references: /Volumes/Dev/worktrees/jolt/jolt-inlines-b/{jolt-inlines/blake2/src/sequence_builder.rs, jolt-inlines/sdk/src/host.rs, crates/jolt-program/src/expand/inline.rs, book/src/how/optimizations/inlines.md}, revm-precompile-34.0.0/src/blake2.rs (algo::compress, SIGMA, IV) in ~/.cargo/registry/src.
Verify by reading code (and running your own checks in a /tmp copy if cheap):
1. Sequence semantics: for each op Blake2bRounds<R>, the emitted rows implement exactly R rounds of RFC 7693 (G function order and indices per round: column step then diagonal step; sigma row i for round i, i < R; rotations 32/24/16/63 via the VirtualXORROT ops — confirm the ops' exact semantics in the tracer (rotate-right after xor) match what G needs; 64-bit wrapping adds). Compare against revm's algo.rs round loop symbol by symbol, not against the author's tests.
2. Memory contract: rs1 = v[16] read then written, rs2 = m[16] read only; all loads precede all stores? (if rs1 == rs2 aliasing is claimed well-defined, check it really is); alignment assumptions; does the sequence write anything outside v[0..16]; are all inline registers released and reset (the book requires all inline virtual registers zeroed at the end — confirm the builder does this and that the golden row count 80R+80 includes the 32 reset rows). No real RISC-V register is modified (rd is x0?).
3. Encoding: opcode 0x2B, funct7 0x02/0x03, funct3 mapping; registration with InlineExtension::External; no collision with stock inlines (opcode 0x0B) — confirm the tracer lookup keys on (opcode, funct3, funct7) so the same funct3 under 0x0B and 0x2B never conflict; the encoding-injective test is real.
4. Routing in crates/core/src/crypto.rs: rounds == 12 && t[1] == 0 → stock inline; rounds == 0 → init + fold; else init (revm's exact statements incl. t1 into v13 and f into v14), q × FULL10, PREFIX_(rounds % 10), fold. Prove compose(rounds) == compress(rounds) for all rounds ≥ 0 including rounds = 10, 20, 12 with t1 ≠ 0, and u32::MAX rounds (gas-capped upstream, but no panic/overflow in the loop counter). Check the init/fold code is not a subtly different transcription (e.g. v14 = !v14 iff f; v13 ^= t1).
5. cfg gating: native build stays on revm software (run-native independent); guest gets the inline; the host links the crate for inventory registration; the InlineSpec tests run the real sequence through the tracer harness against a software reference that is NOT the sequence itself.
6. Prover-side failure modes: none should exist (no advice); confirm no assert rows, no panics reachable on valid input, and that a malicious prover has no freedom (every row is a deterministic instruction).
7. Complexity budget: is the crate the smallest correct shape (const generic R ok?), any duplicated tables (SIGMA/IV copied from jolt-inlines-blake2 or revm — are they asserted equal to revm's in a test?), narration comments, dev-only probes committed, scratch files.
Output: verdict (APPROVE / FIX-THEN-RE-REVIEW / BLOCK) + numbered findings with file:line, severity (blocker / should-fix / nit), evidence, concrete fix. Terse. Finish with ONE pika-cli reply parent --text. Vault ~/.pika/vault read-only. Never edit under /Volumes/Dev/worktrees/jolt. Never set CARGO_TARGET_DIR.
