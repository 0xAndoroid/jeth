Review of inlines-b-bls @ 00fe5f6 came back APPROVE with one should-fix and nits. In /Volumes/Dev/worktrees/jeth/inlines-b-bls (no CARGO_TARGET_DIR; JETH_GUEST_TARGET_DIR=/Volumes/Dev/worktrees/jeth/inlines-b-bls/target/guest; JOLT_PATH=/Volumes/Dev/worktrees/jolt/jolt-inlines-b/target/release/jolt):
STEP 1 — one fix commit: F1 crates/vendor/ark-ff/src/jolt_bls12_381.rs:27,38 — the 48-byte / offset-0 layout of Fp<MontBackend, 6> is guarded only by debug_assert before the *mut u64 cast; make it compile-time: fold size_of::<Fp<Self, N>>() == 48 (and offset 0) into the JOLT_BLS12_381_FQ const (montgomery_backend.rs:605) the way is_fq2 checks its layout, so the hook arm is simply not taken if the layout ever differs. N4: document in is_fq2 that the base backend is assumed to be MontBackend (only one in the graph). Journal nits: column-population text (MULP 22 terms; SOPP2/FP2MUL up to 33; carry register max observed 18 < 2^6) and that the dev-dep ark-ff resolves to the vendored crate (native code byte-identical to the registry). Workspace nextest; pre-commit green.
STEP 2 — WAIT for the bn254 lane to land: poll (every 2–3 min, up to 90 min) until 'git -C /Volumes/Dev/dev/jeth show inlines-b:crates/inlines/bn254/Cargo.toml' succeeds (the bn254 lane is being rebased and merged into inlines-b right now; it edits the same vendored ark-ff files: montgomery_backend.rs if-chain, quadratic_extension.rs arm, lib.rs mod line, Cargo.toml feature, plus workspace members/deps, host Cargo + trace.rs, guest Cargo, crypto.rs). Then rebase inlines-b-bls onto inlines-b; resolve as unions: both hook arms as independent if-arms in the same chain (bn254 N == 4 arm and your N == 6 arm), both mod lines, both features, both workspace members, both extern crates. Your vendor commit 3bf6e2c duplicates the bn254 lane's e11b444 (identical tree) — let the rebase drop it or resolve to identical content; same for the .cargo/config.toml removal.
STEP 3 — on the rebased tree: workspace nextest; rebuild the guest; trace copies of 25905781 (expected 596,202,738 rows = bn254 lane number, since BLS has no calls there; hash 0xf691da3f…, perms 115,373) and the Aztec block /Volumes/Dev/jeth-top50-data/25694235/input.bin (copy to your scratch dir first; report rows before = 1,426,158,765 (your feature-off), your bls-only number 1,407,097,112, and the union number with bn254 — expected well below both; hash 0x1a8f8e57…a0659e, perms 193,806); then all ten blocks (rows, hash-ok, perms-ok).
Do not push. Finish with ONE pika-cli reply parent --text: rebased head sha, tests, the 781 / Aztec / ten-block table, the conflict resolutions.
