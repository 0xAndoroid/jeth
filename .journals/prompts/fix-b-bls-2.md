FOLLOW-UP on inlines-b-bls (finding from the proof-gate lane, which proved and verified your inlines end-to-end — PASS): in a Jolt example guest the BLS FP2MUL hook saves ~0 rows per call: bls Fq2 mul 2,631.1 rows/op hooked vs 2,631.8 compiled, while bn254 Fq2 shows 819.5 vs 1,151.1. The `hot` profile shows 627 rows/op in memcmp + 84 in memcpy inside jolt_bls12_381::is_fq2: `characteristic() == &MODULUS[..]` compiles to a runtime 48-byte memcmp and `NONRESIDUE == -ONE` to a runtime negation + copy, whereas the bn254 guard const-folds. Fix in /Volumes/Dev/worktrees/jeth/inlines-b-bls (first: git fetch nothing — rebase your branch onto inlines-b @ d254322 or later, i.e. `git rebase inlines-b`; it already contains your merged commits, so expect a no-op/fast-forward): make the Fq2 guard const-evaluable exactly like the bn254 one (compare P::BaseField::MODULUS limbs and P::NONRESIDUE limbs as compile-time constants — const fn over BigInt limbs, MontBackend assoc consts; no runtime memcmp, no negation at runtime; if NONRESIDUE == -1 must be checked, compare against the precomputed Montgomery limbs of p−1 as a const). Then prove it folds: (1) the guest ELF symbol/row check — `jeth profile --rows --entries` on a synth BLS_G2ADD or G2MSM block must show no memcmp/memcpy rows attributable to the guard and Fq2 mul rows ≈ 1,875 + subtractions; (2) re-measure the synth configs BLS_G2MSM k1 + k8, BLS_PAIRING k1 + k2, POINTEVAL, MAP_FP2_TO_G2, G2ADD, G1ADD, G1MSM k1 (before = your previous after numbers; report the new after and the new percentage vs feature-off); (3) the Aztec block 25694235 (copy the input from /Volumes/Dev/jeth-top50-data/25694235/input.bin to /Volumes/Dev/jeth-scratch/inlines-b-bls/) rows + hash; (4) ten blocks unchanged (no BLS calls) — 781 rows/hash only is enough; (5) workspace nextest. Also explain why your earlier synth measurements showed −13.9% on G2MSM if the Fp2 hook was not folding (did it fold in the jeth guest but not in the example guest? measure, do not guess: compare Fq2 mul rows/call in the jeth guest before and after this fix).
Environment: no CARGO_TARGET_DIR; JETH_GUEST_TARGET_DIR=/Volumes/Dev/worktrees/jeth/inlines-b-bls/target/guest; JOLT_PATH=/Volumes/Dev/worktrees/jolt/jolt-inlines-b/target/release/jolt; block inputs /Volumes/Dev/jeth-inputs/<block>/input.bin (copy before tracing). One fix commit (Conventional Commits), pre-commit green, do not push. Finish with ONE pika-cli reply parent --text: head sha, the guard change, the fold proof, the re-measured table (old after → new after, % vs off), Aztec rows/hash, 781 rows/hash, tests.
