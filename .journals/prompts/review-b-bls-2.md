# Fresh re-review (round 2, narrow): inlines-b-bls guard fix 92a1093 — the BLS12-381 Fq2 hook guard made const-evaluable
Independent reviewer. Scope: 'git diff 7c88cbf..92a1093' in /Volumes/Dev/worktrees/jeth/inlines-b-bls (one commit; do NOT modify/commit; build without CARGO_TARGET_DIR: cargo nextest run --cargo-quiet --release --workspace --features jeth-host/secp-inline). Context: round 1 APPROVED the lane; the proof-gate lane then found that the Fq2 guard `is_fq2` ran at runtime (memcmp + negation), so the author rewrote it as a const fn over compile-time constants: size/offset checks (BaseField 48 B, QuadExtField 96 B, c0/c1 at 0/48, size_of BigInt == 48), then the base prime field's MODULUS limbs == p and P::NONRESIDUE limbs == MINUS_ONE (Montgomery form of p − 1), read via const raw-pointer casts after the size checks, bound to an assoc const QuadExtField::<P>::JOLT_BLS12_381_FQ2 so the arm folds at monomorphization.
Check, by reading code: (1) the const raw-pointer casts are sound given the checks that precede them (what exactly is cast, from which type, is the source guaranteed to have that layout at const-eval time; any UB under Rust's const-eval rules — transmute-like reads of generic assoc consts, padding, provenance); (2) the guard cannot be TRUE for any type other than BLS12-381 Fq2 over MontBackend (Fp6/Fp12 towers, other 6-limb fields, a hypothetical QuadExtField with the same layout but a different NONRESIDUE/backend) and cannot be FALSE for the real Fq2 (which would silently drop the optimization but remain correct — say which direction each failure mode is); (3) MINUS_ONE is the correct Montgomery representation of −1 for p (check the constant against ark: −Fq::ONE limbs) and the test asserting it is real; (4) the quadratic_extension.rs arm is unchanged in semantics (same Fp2 mul call, same canonicalization), only the predicate moved; (5) native build still bit-identical to upstream (all under cfg riscv64 + feature); (6) nothing else in the diff (journal wording aside). Output: verdict APPROVE / FIX-THEN-RE-REVIEW / BLOCK + numbered findings with file:line, severity, evidence, fix. Terse. ONE pika-cli reply parent --text. Vault read-only; never edit under /Volumes/Dev/worktrees/jolt; never set CARGO_TARGET_DIR.
