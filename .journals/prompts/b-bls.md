# Lane B-bls: custom Jolt inlines for BLS12-381 Fp multiplication (6-limb MULP, SOPP2, optionally Fp2 mul) hooked into ark-ff for the BLS12-381 precompiles (G1/G2 add, MSM, pairing, map-to-curve) and KZG point evaluation
LANE_WT=/Volumes/Dev/worktrees/jeth/inlines-b-bls · LANE_BRANCH=inlines-b-bls · LANE_TARGET=jeth-inlines-b-bls · FUNCT7=0x01 (opcode 0x2B)
You are a Jolt/jeth engineer. BLS12-381 ops are the top adversarial offenders after P256VERIFY (max-c/g study: BLS_G2MSM k1 439 c/g, MAP_FP_TO_G1 296, PAIRING k1 296, G1MSM k1 285, POINTEVAL 283, G1ADD 260, G2ADD 252) and appear in 16 of the 50 heaviest mainnet blocks (25694235: BLS + bn254 field ops = 27.5% of 1.43B rows). All of them bottom out in ark-bls12-381 0.5 Fp (MontBackend<FpConfig, 6>, 381-bit modulus, 6 limbs) mul_assign / square_in_place / sum_of_products, and Fp2 on top. Read .journals/bn254-fq-inline-design.md for the row model on this ISA (8 rows per 64×64 partial-product pair incl. carries; 2-register ping-pong accumulator; 80 inline registers; full 64-bit immediates) and jolt-inlines/p256 for the advice-quotient alternative; the 6-limb case has 36 + 36 partial products for a·b + m·p (deterministic Montgomery) — estimate rows for MULP, SQUAREP, SOPP2 (ark's sum_of_products for 381-bit uses chunk_size = 2·(384−381)−1 = 5 → up to 5 fused terms; check which M values ark's Fp2/pairing code actually calls) and the fused Fp2 mul, against the compiled ark code's dynamic rows/call measured with jeth profile --rows --entries on a synth block that calls the BLS precompiles (build it with the harness: BLS_G1ADD, BLS_G2ADD, BLS_G1MSM/k1, BLS_G2MSM/k1, BLS_PAIRING/k1, BLS_MAP_FP_TO_G1, POINTEVAL configs exist in .journals/opcode-max-cg/cases*.py + vectors.json).
PRE-GATE FIRST (≤ 1.5 h): measured rows/call and call counts per op on those blocks; projected saving per precompile; bar: ≥ 12% of the precompile's rows for at least G2MSM/pairing/POINTEVAL, else NO-GO with numbers. Register budget must fit (6-limb a, b, p, m, accumulators, temporaries ≤ 80 regs) — show the allocation.
Integration mirrors the bn254 lane, which runs in parallel and owns the vendored ark-ff scaffolding: vendor registry ark-ff 0.5.0 into crates/vendor/ark-ff as ONE commit with content UNCHANGED ("chore(vendor): ark-ff 0.5.0 for guest field hooks" — identical bytes so the two lanes merge cleanly), [patch.crates-io] in crates/guest/Cargo.toml and the root workspace, then your hook as a separate commit: in montgomery_backend.rs's FpConfig impl for MontBackend<T, N> add ONE if-arm keyed by a const check (N == 6 && T::MODULUS limbs == the BLS12-381 p) behind cfg(all(target_arch = "riscv64", feature = "jolt-bls12-381-inline")); do not restructure the file. Also cover Fp2 (ark quadratic_extension) only if the fused op pays. Verify (cargo tree) the patch replaces ark-ff for revm-precompile's bls12_381 + kzg modules in the guest graph and that the a16z arkworks fork used by jolt-sdk is untouched; confirm which backend revm-precompile 34 uses for BLS and KZG in the guest (no blst / c-kzg on riscv → ark) before assuming.
Gates: synth configs BLS_G1ADD, BLS_G2ADD, BLS_G1MSM/k1 + k8, BLS_G2MSM/k1 + k8, BLS_PAIRING/k1 + k2, BLS_MAP_FP_TO_G1, BLS_MAP_FP2_TO_G2, POINTEVAL (before/after rows, exact); ten blocks (few or no BLS calls expected — rows must stay equal where there are none, hashes + perms exact); block 25694235 from /Volumes/Dev/jeth-top50-data/25694235/input.bin if present (the Aztec BLS block: rows before/after + hash == its trace-summary); forged blocks: G1/G2 infinity encodings, points not on curve / not in subgroup, coordinates ≥ p, scalar 0 / r / r−1 / 2^256−1, MSM with k=0 / duplicated points, pairing with infinity operands, KZG with wrong proof / z ≥ r / infinity commitment — guest hash == native hash on each. InlineSpec tests: random 10k + edges {0, 1, R, p−1, (p−1)², limb u64::MAX, t ≥ p cases, a == b, aliasing}. Golden row counts asserted.
