[toolchain]
# Match Jolt's toolchain (keccak-9340a77 pins 1.95); riscv target for the guest.
channel = "1.95"
targets = ["riscv64imac-unknown-none-elf"]
components = ["cargo", "rustc", "clippy", "rustfmt", "rust-src"]
