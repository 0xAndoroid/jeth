[toolchain]
# Match Jolt's toolchain (main-2026-09-04 pins 1.95); riscv target for the guest.
channel = "1.95"
targets = ["riscv64imac-unknown-none-elf"]
components = ["cargo", "rustc", "clippy", "rustfmt", "rust-src"]
